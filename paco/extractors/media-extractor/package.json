{
  "name": "media-extractor",
  "version": "1.0.0",
  "description": "Turns video/audio bytes of an asserted format into a bounded set of preview images: a 5-frame filmstrip for video, or a waveform + spectrogram (plus embedded cover art if present) for audio. Never attempts playback. { kind, metadata, items, html, error } out, never throws.",
  "main": "src/mediaExtractor.js",
  "scripts": {
    "test": "node --test"
  },
  "keywords": ["video", "audio", "ffmpeg", "filmstrip", "waveform", "spectrogram", "preview"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0",
    "sharp": "^0.35.3"
  }
}
